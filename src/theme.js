import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      main: "#CE2B37",
    },
    secondary: {
      main: "#ffffff",
    },
    success: {
      main: "#009246",
    },
  },
});

export default theme;
